<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Warna latar belakang tombol -->
    <solid android:color="#2EAA76" />

    <!-- Radius sudut tombol -->
    <corners android:radius="16dp" />

    <!-- Ukuran stroke/garis tepi (opsional) -->
    <!-- <stroke android:width="1dp" android:color="#229E6B" /> -->

    <!-- Padding dalam tombol (opsional) -->
    <padding
        android:left="16dp"
        android:top="12dp"
        android:right="16dp"
        android:bottom="12dp" />

    <!-- Gradien (opsional) -->
    <!--
    <gradient
        android:startColor="#33BB7A"
        android:endColor="#2EAA76"
        android:angle="270" />
    -->
</shape>